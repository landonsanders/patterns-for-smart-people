var flag = true, 
    result;

// A condition can be an expression

result = (flag)? 'The value is true.': 'The value is false.';


// A condition can be a statement

if (flag) {
    result = 'The value is true.';
} else {
    result = 'The value is false.';
}